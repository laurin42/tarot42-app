import "expo-router/entry";

// All other code, including Google Sign-In configuration, temporarily removed.
// If this works, we reintroduce Google Sign-In configuration carefully.
export default function App() {
  // Expo Router's entry point should handle rendering the root layout (app/_layout.tsx)
  return null;
}
